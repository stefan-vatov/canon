# Glossary

- fetch function -> caller-supplied zero-argument callable performing one
  request attempt
- attempt -> one invocation of the fetch function, including the first call
- retry -> any attempt after the first
