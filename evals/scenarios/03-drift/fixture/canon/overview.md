# Project Overview

This project is a small HTTP client wrapper that adds retry behavior on top
of a caller-supplied fetch function. It has no other responsibilities. The
single domain is [client](client/overview.md).
