{
  "test_cmd": "uv run python -m unittest discover -s . -p 'test_*.py'",
  "rules": [
    {
      "id": "validate_promo_implemented",
      "glob": "orders.py",
      "must_regex": "def validate_promo\\(",
      "description": "helper exists"
    }
  ],
  "holdout": {
    "dir": "holdout",
    "test_cmd": "uv run python -m unittest holdout_t06"
  }
}
