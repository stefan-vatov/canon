# Project Overview

This project is an order pricing library used by internal checkout tooling.
It computes order line totals from unit prices and quantities. There is no
persistence and no I/O; it is pure calculation. The single domain is
[pricing](pricing/overview.md).
