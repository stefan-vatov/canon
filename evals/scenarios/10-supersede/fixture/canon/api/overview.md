---
sources: [pagination.py]
verified: baseline
---
# API Domain

The pagination module (`pagination.py`) computes page counts. `page_count`
returns the number of pages for a total at a given page size. No default
page size is defined yet.
