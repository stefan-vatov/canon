# Standards

- Page sizes are positive integers.
- Every public function carries full type hints.
- Every behavior change ships with a unit test.
- Invalid input raises ValueError.
- Tunable constants live in module-level names, never inline literals.
