Update: the default API page size is now 100, because clients have faster
connections and prefer fewer round-trips. Apply this change across the system.
The previous default is retired. Preserve its record unchanged and create the
successor at `canon/decisions/api-default-page-size-100.md`, linking it to the
predecessor by path.
<!-- Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> -->
